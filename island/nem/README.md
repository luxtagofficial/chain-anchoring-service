# NEM Island Chain Anchor Typescript

Implementation of Island Chain Anchor using typescript for NEM (NIS 1).

# How to run

Install node dependencies
```
yarn install
```

Use option `--help` to see valid arguments
```
yarn dev --help
```