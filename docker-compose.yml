---
version: "2.1"

services:
  island:
    build:
      context: .
      dockerfile: island/${ISLAND_TYPE:-nem2}/Dockerfile
    env_file: .env
  ship:
    build:
      context: .
      dockerfile: ship/${SHIP_TYPE:-nem2}/Dockerfile
    env_file: .env
    depends_on:
      - island